import { useUpdateDocumentTitle, useUpdateDocumentTitleProps } from "./useUpdateDocTitle";

const UpdateDocTitle = (props: useUpdateDocumentTitleProps) => {
    useUpdateDocumentTitle(props);

    return (
        <>
        </>
    );
}

export default UpdateDocTitle;