
export const __peerConfig = {
    iceServers:[
        {
            // urls: __apiUrls.stunServer,
            // urls: [
            //     __urls.stunServer,
            //     __urls.stunServer1,
            // ]
        }
    ]
};